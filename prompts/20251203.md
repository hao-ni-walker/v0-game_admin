玩家列表管理
用户id，用户名（username），邮箱(email)，状态(status)，上级（direct_superior_id），vip等级（user_vip_info.vip_level），注册时间(created_at)，最后登录时间(last_login)，钱包 balance(user_wallets.balance) bonus(user_wallets.bonus) credit(user_wallets.credit) frozen_balance(user_wallets.frozen_balance) total_deposit(user_wallets.total_deposit) total_withdraw(user_wallets.total_withdraw) total_bet(user_wallets.total_bet) total_win(user_wallets.total_win)

可以修改密码

查看操作记录
查看大转盘spin次数
发送通知
查看签到记录
参与活动
修改rtp

游戏列表
游戏id，平台所属，游戏标识，游戏名称，分类，供应商，最小下注，最大下注，RTP，特性，状态，是否推荐，是否新品

操作 删除，编辑

活动列表
活动id，活动编码，活动名称，活动类型，活动状态，活动时间，展示时间，优先级，创建时间，更新时间
查看详情- 查看规则和奖励
编辑

礼包列表
礼包id，分类，名称，数量，状态，要求，限制，礼品明细，创建时间，更新时间
操作
删除，编辑

Banner列表
id，名称，图片，跳转地址，跳转方式，页面，位置，排序，展示时间，状态，创建时间，更新时间
操作
删除，编辑，

通告列表

储值订单列表

提现订单列表

数据分析

游戏流水

运营报表

用户管理

角色管理

权限管理

日志管理

系统参数配置

支付渠道管理

VIP 等级管理

工单管理

平台管理
