# BTC 秒合约 MiniApp — 后台管理端产品功能需求文档

**文档版本**：v1.0  
**产品类型**：Web 管理后台  
**更新日期**：2026-06-09  
**文档范围**：运营管理、风险控制、结算审计、用户管理

---

## 目录

1. [后台整体架构](#1-后台整体架构)
2. [仪表盘 Dashboard](#2-仪表盘-dashboard)
3. [核心风控系统](#3-核心风控系统)
4. [赔率管理模块](#4-赔率管理模块)
5. [下单限额与截止时间控制](#5-下单限额与截止时间控制)
6. [资金池与流动性监控](#6-资金池与流动性监控)
7. [结算与开奖管理](#7-结算与开奖管理)
8. [用户管理模块](#8-用户管理模块)
9. [异常行为检测](#9-异常行为检测)
10. [充提审核模块](#10-充提审核模块)
11. [报表与审计日志](#11-报表与审计日志)
12. [系统配置模块](#12-系统配置模块)
13. [权限与角色管理](#13-权限与角色管理)
14. [风控核心算法说明](#14-风控核心算法说明)

---

## 1. 后台整体架构

```
后台管理系统
├── 仪表盘              ← 实时经营概览
├── 风控中心
│   ├── 实时风险监控
│   ├── 赔率管理（自动 + 手动）
│   ├── 限额配置
│   ├── 单边行情控制
│   └── 异常账户检测
├── 结算中心
│   ├── 开奖记录
│   ├── 结算审计
│   └── 异常结算处理
├── 用户管理
│   ├── 用户列表
│   ├── 账户操作
│   └── 黑名单管理
├── 资金管理
│   ├── 充值审核
│   ├── 提现审核
│   └── 资金池监控
├── 报表中心
│   ├── 盈亏报表
│   ├── 交易报表
│   └── 审计日志
└── 系统配置
    ├── 参数配置
    ├── 角色权限
    └── 操作日志
```

---

## 2. 仪表盘 Dashboard

### 2.1 实时核心指标（自动刷新，间隔 5 秒）

| 指标 | 说明 |
|------|------|
| 平台实时净风险敞口 | 当前所有未结算订单中，买涨方向总金额 − 买跌方向总金额（正值平台面临上涨风险，负值面临下跌风险）|
| 当日总流水 | 所有已下单金额之和 |
| 当日平台盈利 | 已结算订单中平台净收益 |
| 在线用户数 | 当前活跃 MiniApp 用户数 |
| 待结算订单数 | 所有未到期订单总数 |
| 资金池余额 | 平台可用流动性资金 |

### 2.2 风险预警看板

- 风险敞口超阈值 → 红色警报
- 资金池低于安全线 → 橙色预警
- 单边行情触发 → 黄色提示（含受影响周期）
- 异常账户触发 → 蓝色提示

### 2.3 实时订单流

展示最新下单动态（滚动列表），包含：用户 ID（脱敏）/ 方向 / 周期 / 金额 / 时间

---

## 3. 核心风控系统

### 3.1 风控总体设计原则

平台盈利模型基于**赔率差**（庄家优势）和**双向对冲**：

- 平台设定赔率低于公平赔率（如 1 分钟公平赔率为 50%，平台设为 35%）
- 买涨与买跌资金自然对冲，超额方向由平台承担风险敞口
- 风控目标：将平台净风险敞口控制在**安全阈值内**，超出时自动或手动干预

### 3.2 风险敞口实时计算

**计算公式：**

```
净风险敞口（某周期）= Σ 买涨金额 × (1 + 赔率) − Σ 买跌金额 × (1 + 赔率)

简化计算：
  多头暴露 = 买涨总金额（若 BTC 上涨，平台需赔付）
  空头暴露 = 买跌总金额（若 BTC 下跌，平台需赔付）
  净敞口   = |多头暴露 − 空头暴露|
  平台最大损失 = max(多头暴露, 空头暴露) × 赔率
```

**示例：**
```
1分钟周期内：
  买涨总金额 = $8,000，赔率 35%
  买跌总金额 = $2,000，赔率 35%
  若 BTC 上涨 → 平台赔付 $8,000 × 35% = $2,800，收入 $2,000
  平台净亏损 = $2,800 − $2,000 = $800
  → 触发单边行情风控介入
```

### 3.3 风险敞口分级阈值

| 级别 | 净敞口范围 | 系统行为 |
|------|-----------|---------|
| 正常 | < $3,000 | 无干预，正常运行 |
| 预警 | $3,000 ~ $6,000 | 仪表盘橙色预警，通知值班运营 |
| 高风险 | $6,000 ~ $10,000 | 自动降低对应方向赔率（见 §4.3）|
| 极限 | > $10,000 | 自动触发赔率清零，锁定该周期新单 |

> 以上阈值为默认配置，可在系统配置模块按平台资金规模调整。

### 3.4 单边行情识别与处理

**单边行情判定条件（满足任一即触发）：**

```
条件 A（价格维度）：
  过去 N 分钟内，BTC 价格单方向涨跌幅 ≥ X%
  默认：N = 5 分钟，X = 1.5%

条件 B（下单维度）：
  某周期内，买涨（或买跌）订单金额占比 ≥ Y%
  默认：Y = 75%（即买涨金额 ÷ 总金额 ≥ 75%）

条件 C（净敞口维度）：
  净风险敞口达到"极限"级别（> $10,000）
```

**触发后系统行为（自动）：**

1. 对应周期赔率设为 0%
2. 前端展示单边行情提示条
3. 仪表盘记录触发事件（时间 / 触发条件 / 涉及周期）
4. 向值班运营推送 Telegram 通知
5. 允许用户继续下单（赔率为 0，平台无赔付压力）

**恢复条件（自动或手动）：**

```
自动恢复条件（同时满足）：
  - 价格涨跌幅回落至 X% 以内，持续 M 分钟（默认 M = 3）
  - 买涨/买跌占比恢复至 Y% 以内
  - 净风险敞口降至"预警"级别以下

手动恢复：
  - 运营人员在后台点击"恢复赔率"按钮
  - 需二次确认 + 操作备注
  - 记录操作人员 / 时间 / 原因
```

---

## 4. 赔率管理模块

### 4.1 基础赔率配置

| 周期 | 基础赔率 | 可配置范围 |
|------|---------|-----------|
| 1 分钟 | 35% | 0% ~ 60% |
| 3 分钟 | 50% | 0% ~ 70% |
| 5 分钟 | 65% | 0% ~ 80% |
| 10 分钟 | 80% | 0% ~ 90% |

修改基础赔率需：管理员权限 + 二次确认 + 生效时间设定（立即生效 / 下一周期生效）

### 4.2 赔率手动操作

| 操作 | 触发方式 | 权限要求 | 记录要求 |
|------|---------|---------|---------|
| 手动清零（单周期） | 按钮点击 | 风控专员以上 | 必须填写原因 |
| 手动清零（全周期） | 按钮点击 + 二次确认 | 风控主管以上 | 必须填写原因 |
| 手动恢复（单周期） | 按钮点击 | 风控专员以上 | 必须填写原因 |
| 手动恢复（全周期） | 按钮点击 + 二次确认 | 风控主管以上 | 必须填写原因 |

### 4.3 赔率动态调节算法

当净风险敞口进入"高风险"区间时，系统自动降低**占优方向**的赔率，引导资金流向劣势方向，实现对冲：

```
动态赔率计算逻辑：

设：
  base_odds    = 基础赔率（如 35%）
  long_amount  = 当期买涨总金额
  short_amount = 当期买跌总金额
  total        = long_amount + short_amount
  imbalance    = |long_amount - short_amount| / total  ← 资金倾斜度，0~1

动态赔率调节：
  若 long_amount > short_amount（买涨方向资金过多）：
    买涨赔率 = base_odds × (1 - imbalance × k)   ← 降低买涨赔率
    买跌赔率 = base_odds × (1 + imbalance × k)   ← 提高买跌赔率（吸引资金）

  k = 调节系数，默认 0.5，范围 0.1 ~ 1.0（可在系统配置中调整）

  赔率下限保护：任何动态调节后赔率不低于 5%（除非触发清零条件）
  赔率上限保护：动态调节后赔率不高于基础赔率 × 1.5

示例：
  基础赔率 35%，买涨 $7,000，买跌 $3,000，总计 $10,000
  imbalance = |7000 - 3000| / 10000 = 0.4
  买涨赔率 = 35% × (1 - 0.4 × 0.5) = 35% × 0.8 = 28%
  买跌赔率 = 35% × (1 + 0.4 × 0.5) = 35% × 1.2 = 42%
  → 降低买涨赔率，吸引买跌资金入场，对冲平台风险
```

**注意：** 动态赔率变化前端实时可见，用户可基于实时赔率自主决策，平台不隐藏调节过程。

### 4.4 赔率历史记录

所有赔率变动（自动或手动）均记录：

- 变动时间
- 变动前 / 后赔率
- 触发方式（自动规则 / 手动操作）
- 操作人员（手动时）
- 触发原因（规则条件 or 备注）

---

## 5. 下单限额与截止时间控制

### 5.1 单笔下单限额

| 参数 | 默认值 | 说明 |
|------|-------|------|
| 单笔最低下单额 | $1 | 低于此值拒绝下单 |
| 单笔最高下单额 | $500 | 超出此值拒绝下单，前端提示 |
| VIP 用户单笔上限 | $2,000 | 可按用户等级配置 |

### 5.2 单用户周期限额

| 参数 | 默认值 | 说明 |
|------|-------|------|
| 单用户单日下单总额上限 | $5,000 | 超出当日禁止继续下单 |
| 单用户同时持仓订单数上限 | 20 笔 | 超出时拒绝新单 |
| 单用户单周期最大持仓额 | $1,000 | 同一周期内该用户总持仓不超过此值 |

### 5.3 平台全局限额

| 参数 | 默认值 | 说明 |
|------|-------|------|
| 单周期全平台总下单额上限 | $50,000 | 超出后该周期不再接受新单 |
| 单方向（买涨/买跌）单周期上限 | $30,000 | 防止单方向过度集中 |

### 5.4 截止下单时间控制

```
每期订单生命周期示意（以 3 分钟周期为例）：

  T+0:00  ← 本期开始，开放下单
  T+2:45  ← 截止下单（提前 15 秒）
  T+3:00  ← 本期结算，以收盘价开奖
  T+3:05  ← 结算完成，盈亏到账

截止提前量（可配置）：
  1 分钟周期：提前 10 秒截止
  3 分钟周期：提前 15 秒截止
  5 分钟周期：提前 20 秒截止
  10 分钟周期：提前 30 秒截止
```

截止时间到达后：
- 前端下单按钮显示倒计时并锁定
- 服务端拒绝该周期内新的下单请求
- 已在截止前提交的订单正常参与结算

---

## 6. 资金池与流动性监控

### 6.1 资金池构成

```
平台资金池 = 用户充值总额 − 用户提现总额 − 平台已赔付总额 + 平台累计收入

  平台累计收入来源：
    ① 赔率差收益（用户亏损的本金归平台）
    ② 提现手续费
    ③ 充值汇率差（法币转换）
```

### 6.2 安全线配置

| 级别 | 资金池余额 | 系统行为 |
|------|-----------|---------|
| 健康 | ≥ $50,000 | 正常运营 |
| 预警 | $20,000 ~ $50,000 | 推送预警通知，限制新用户充值上限 |
| 危险 | $10,000 ~ $20,000 | 全平台单笔下单上限降至 $100，推送紧急通知 |
| 停止 | < $10,000 | 自动暂停新单接受，仅处理存量订单结算和提现 |

### 6.3 最大赔付压力计算

实时计算**当前所有未结算订单若全部押注方向正确时，平台需赔付的最大金额**：

```
最大赔付压力 = Σ（每笔未结算订单金额 × 对应赔率）

当 最大赔付压力 > 资金池余额 × 安全系数（默认 80%）时：
  → 触发"危险"级别预警
  → 自动提高各周期截止提前量（减少新单进入）
```

### 6.4 资金流水监控

- 实时展示充值 / 提现 / 赔付 / 收入的资金流向图
- 支持按日 / 周 / 月查看资金净流量
- 异常大额充值或提现（单笔 > $10,000）自动标记人工复核

---

## 7. 结算与开奖管理

### 7.1 开奖价格来源

```
开奖价格取自：合作行情数据源（如 Binance / Coinbase API）
  取价逻辑：每期结算时间点的 BTC/USD 1 分钟收盘价
  备用数据源：当主数据源异常时，自动切换备用源
  异常判定：主备数据源价格差异 > 0.1% 时，人工介入确认
```

### 7.2 结算逻辑

```
开奖判定：
  开奖价格 > 开单价格 → 买涨方向获奖
  开奖价格 < 开单价格 → 买跌方向获奖
  开奖价格 = 开单价格 → 平局，本金全额退回

  开单价格 = 用户下单时的 BTC/USD 实时价格（服务端记录，毫秒级精度）

赔付计算：
  获奖用户到账金额 = 本金 + 本金 × 赔率
  失败用户扣除本金

赔率为 0 时：
  无论涨跌，用户均不获得盈利
  本金原路退回（赔率清零期间下单，用户不亏本金，但无收益）
```

### 7.3 批量结算流程

```
每期结算任务（自动触发）：

Step 1  获取结算时间点的开奖价格
Step 2  查询本期所有未结算订单
Step 3  逐笔判定涨跌结果
Step 4  批量更新订单状态（获奖 / 亏损 / 平局）
Step 5  批量更新用户余额（加减账）
Step 6  写入结算审计日志（含开奖价、每笔明细）
Step 7  推送结果通知至前端（WebSocket）
Step 8  生成本期结算汇总报告

异常处理：
  若 Step 1 取价失败 → 延迟 5 秒重试，最多 3 次
  若 3 次均失败 → 暂停本期结算，触发人工介入流程
  人工介入后，管理员可手动输入开奖价格并附上数据来源截图
```

### 7.4 结算异常处理

| 异常类型 | 处理方式 |
|---------|---------|
| 价格数据源异常 | 暂停结算，人工确认价格后手动触发 |
| 系统延迟导致超时 | 以实际结算时刻价格为准，记录延迟原因 |
| 用户投诉结算有误 | 调取审计日志核查，7 日内答复 |
| 批量结算中途失败 | 事务回滚，全部订单恢复待结算状态，重新执行 |

---

## 8. 用户管理模块

### 8.1 用户列表

**查询字段：**
- 用户 ID / Telegram ID / 昵称
- 注册时间 / 最后活跃时间
- 账户余额
- 累计充值 / 提现 / 盈亏
- 账户状态（正常 / 预警 / 冻结 / 黑名单）

**批量操作：**
- 批量调整单日下单限额
- 批量冻结 / 解冻账户

### 8.2 用户详情页

| 模块 | 内容 |
|------|------|
| 基本信息 | UID / TG ID / 注册 IP / 绑定钱包地址 |
| 资金明细 | 充值记录 / 提现记录 / 赔付记录 / 余额变动明细 |
| 交易记录 | 全部历史订单，支持筛选 |
| 风控标记 | 当前触发的风控规则列表 |
| 操作日志 | 管理员对该账户的所有操作记录 |

### 8.3 账户操作

| 操作 | 权限 | 说明 |
|------|------|------|
| 调整余额（加 / 减） | 主管以上 | 需填写原因，记录入审计日志 |
| 临时提升单笔限额 | 风控专员 | 指定用户、指定有效期 |
| 冻结账户 | 风控专员 | 禁止该账户下单、充值、提现 |
| 解冻账户 | 风控主管 | 需填写解冻理由 |
| 加入黑名单 | 风控主管 | 永久禁止，需管理员审批 |
| 人工退款 | 主管以上 | 针对结算异议的补偿操作 |

---

## 9. 异常行为检测

### 9.1 检测规则

**规则 A：高频下单检测**
```
条件：同一用户在 60 秒内下单次数 ≥ 10 笔
处理：标记预警，降低该用户单笔上限至 $50，通知运营
```

**规则 B：套利模式检测**
```
条件：同一用户在同一周期同时持有买涨和买跌订单（双向对冲）
处理：标记记录，累计 3 次触发账户预警，运营人工复查
```

**规则 C：多账户关联检测**
```
条件：相同设备指纹 / IP / 钱包地址 关联 ≥ 3 个账户
处理：关联账户组标记，人工复查是否为刷单行为
```

**规则 D：大额异常下单**
```
条件：单笔下单金额 > 日均下单额 × 10 倍
处理：自动暂扣订单 30 秒，风控系统审查后放行或拒绝
```

**规则 E：提现异常检测**
```
条件：充值后 1 小时内未交易直接申请全额提现
处理：标记人工复核，疑似搬砖套利或洗钱预防
```

### 9.2 风控规则配置

所有检测规则的阈值均可在后台配置界面调整，无需修改代码：

| 参数 | 默认值 | 可调范围 |
|------|-------|---------|
| 高频检测时间窗口 | 60 秒 | 30 ~ 300 秒 |
| 高频检测笔数阈值 | 10 笔 | 5 ~ 50 笔 |
| 大额异常倍数阈值 | 10 倍 | 3 ~ 20 倍 |
| 提现观察期 | 1 小时 | 0 ~ 24 小时 |

---

## 10. 充提审核模块

### 10.1 充值管理

| 类型 | 处理方式 | 审核要求 |
|------|---------|---------|
| TON 链上充值 | 链上确认后自动到账 | 无需人工审核 |
| WalletConnect USDT | 链上确认后自动到账 | 无需人工审核 |
| 法币支付 | 支付商回调后自动到账 | 无需人工审核 |
| 单笔 > $5,000 充值 | 自动到账后标记 | 人工事后复核 |

### 10.2 提现管理

| 流程步骤 | 说明 |
|---------|------|
| 用户发起提现 | 系统自动检查余额、黑名单、每日限额 |
| 自动风控审查 | 触发规则 E 的提现进入人工队列 |
| 自动广播 | 通过风控的提现自动构建链上交易并广播 |
| 到账确认 | 链上确认后更新提现状态为"已到账" |
| 人工队列 | 风控标记的提现由专员在 4 小时内处理 |

### 10.3 提现人工审核界面

- 展示：用户信息 / 提现金额 / 钱包地址 / 账户历史 / 风控标记原因
- 操作：通过放行 / 拒绝（填写原因，通知用户）/ 加入黑名单
- SLA：标记提现 4 小时内必须处理，超时系统自动升级通知

---

## 11. 报表与审计日志

### 11.1 经营日报（自动生成）

每日 00:00 自动生成前一日报表，包含：

| 指标 | 说明 |
|------|------|
| 总下单笔数 / 金额 | 分周期统计 |
| 平台总收入 | 赔率差收益 + 手续费 |
| 总赔付金额 | 用户获奖总额 |
| 净利润 | 收入 − 赔付 |
| 胜率（平台视角） | 平台获利笔数 / 总结算笔数 |
| 充值总额 / 提现总额 | — |
| 新注册用户数 / 活跃用户数 | — |
| 风控触发次数 | 分规则统计 |

### 11.2 结算审计日志

每笔结算均记录完整链路，支持精确还原：

```
结算记录字段：
  order_id        订单唯一 ID
  user_id         用户 ID
  period          期权周期
  direction       买涨 / 买跌
  amount          下单金额
  open_price      开单价格（含毫秒级时间戳）
  close_price     开奖价格（含毫秒级时间戳）
  odds            结算时赔率
  result          获奖 / 亏损 / 平局
  payout          实际到账金额
  settled_at      结算时间
  price_source    价格数据来源（API 名称 + 原始响应）
```

审计日志**不可删除、不可修改**，仅支持查询，保留 3 年。

### 11.3 操作审计日志

所有管理员操作均记录：

- 操作人员 / 角色
- 操作类型（赔率修改 / 用户冻结 / 手动退款等）
- 操作时间
- 操作前后数值对比
- 操作原因（强制填写）
- 操作 IP

---

## 12. 系统配置模块

### 12.1 全局参数配置

| 参数类别 | 可配置项 |
|---------|---------|
| 赔率 | 各周期基础赔率、动态调节系数 k |
| 限额 | 单笔上下限、日限额、全平台限额 |
| 截止时间 | 各周期截止提前量 |
| 风险阈值 | 净敞口分级阈值、资金池安全线 |
| 单边行情 | 触发条件参数（N、X%、Y%）、自动恢复参数 |
| 异常检测 | 各规则触发阈值 |
| 提现 | 最低提现额、手续费率、每日限额 |

### 12.2 价格数据源配置

- 主数据源选择（Binance / Coinbase / OKX）
- 备用数据源配置
- 切换条件配置（差异阈值、超时时间）
- 数据源可用性监控

### 12.3 通知配置

- 风控触发通知接收人（Telegram 账号）
- 各级别告警的通知方式（Telegram 消息 / 邮件）
- 静默时间配置（避免深夜非紧急通知打扰）

---

## 13. 权限与角色管理

### 13.1 角色定义

| 角色 | 权限范围 |
|------|---------|
| 超级管理员 | 全部权限，含角色管理、黑名单、系统配置 |
| 风控主管 | 赔率管理（含清零/恢复）、用户冻结/解冻、提现审核、查看全部报表 |
| 风控专员 | 赔率查看、单周期手动清零/恢复、异常账户标记、提现审核 |
| 财务 | 充提记录查看、报表查看、不可操作用户账户 |
| 客服 | 用户基本信息查看、订单查看、不可操作资金和赔率 |
| 只读 | 仪表盘和报表查看，无任何操作权限 |

### 13.2 敏感操作双人复核

以下操作需两名管理员（发起人 + 审批人）才能生效：

- 全周期赔率清零
- 单用户余额调整 > $1,000
- 账户加入黑名单
- 基础赔率修改
- 资金池安全线阈值修改

---

## 14. 风控核心算法说明

### 14.1 算法总览

```
风控系统由三层算法构成：

Layer 1 — 实时净敞口监控（毫秒级）
  每笔新订单进入时立即更新净敞口计算
  超阈值时触发 Layer 2

Layer 2 — 动态赔率调节（秒级）
  通过调整赔率引导双向资金均衡
  极端情况下赔率降至 0

Layer 3 — 单边行情识别（分钟级）
  结合价格走势 + 订单方向分布综合判断
  触发后联动 Layer 2 进行清零保护
```

### 14.2 平台期望收益模型

```
设平台赔率为 r（如 35%），假设用户随机下注（涨跌各 50%）：

每 $100 下注：
  平台收入：亏损方本金 $50
  平台赔付：获奖方本金 $50 + 盈利 $50 × 35% = $67.5
  平台净收益：$50 − $67.5 × (50/100) = $50 − $33.75 = $16.25
  理论利润率：16.25%

双向资金越均衡，平台实际收益越接近理论值
净敞口越大，平台面临的方向性风险越高
风控核心目标 = 将净敞口维持在安全范围内，保障理论利润率稳定实现
```

### 14.3 赔率清零的业务逻辑

赔率清零不是"平台不给用户赢"，而是：

1. 单边行情下，BTC 走势高度可预测，用户几乎全部押注同一方向
2. 此时平台面临极大单边赔付风险，赔率差无法覆盖赔付压力
3. 将赔率设为 0 相当于告知用户：此刻下单即便预测正确也无奖励
4. 用户知晓情况后可自主选择不下单，本金不受损失
5. 平台在极端行情下通过此机制保障流动性和正常运营

**前端必须明确展示赔率为 0 的状态，用户在知情情况下决策，保障用户知情权。**

---

*本文档描述后台管理端所有功能模块。客户端用户侧功能详见《客户端产品功能需求文档》。所有风控机制均以平台合规运营为前提，结算规则透明可查，不涉及隐性操控用户资金的机制。*
